import { StyleSheet } from 'react-native';

const Step2Styles = StyleSheet.create({
    container: {
        alignItems: 'center',
      },
      title: {
        fontSize: 16,
        fontWeight: 'bold',
        marginBottom: 10,
      },
});

export default Step2Styles;
